CREATE TABLE car (
 id BIGINT AUTO_INCREMENT NOT NULL,
 model VARCHAR(255),
 brand VARCHAR(255),
 color VARCHAR(255),
 type VARCHAR(255),
 price DOUBLE,
 manufacture_year INTEGER,
 PRIMARY KEY (id)
);